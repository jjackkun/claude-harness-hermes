# presets/tools/terminal-paste-image.conf
# VSCode 익스텐션: Terminal Paste Image
# 터미널에서 클립보드 이미지를 파일로 붙여넣기 할 수 있는 익스텐션.
# project-claude.sh 가 `code --install-extension` 으로 자동 설치한다.

VSCODE_EXTENSIONS+=("mushan.vscode-paste-image")
