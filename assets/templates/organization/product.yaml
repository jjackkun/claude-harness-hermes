# 제품 개발 조직 — 직군은 하는 일, 수직은 지시·승인, 단위는 소속(코드 경로로 묶는다).
# 값만 담는다. 규칙은 없다. 이름을 바꿔도 unit_id 가 같으면 그 단위의 스킬·이력이 이어진다.
# unit_id 가 비어 있으면 설치기·검증기가 UUIDv7 을 부여해 써 넣는다.
discipline: [기획, 디자인, 퍼블리싱, 프론트엔드, 백엔드, QA, QC]
rank: [리드, 담당]   # 위에서 아래 순. 맨 위 human 은 기계가 붙인다
unit:
  공통: {code: [src/components/shared/**, src/lib/**]}
