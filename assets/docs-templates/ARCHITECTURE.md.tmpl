# ARCHITECTURE.md — {{PROJECT_NAME}}

도메인·패키지 레이어링의 **최상위 맵**.
PDF 5쪽: "ARCHITECTURE.md 는 도메인과 패키지 레이어링의 최상위 맵을 제공한다."
PDF 8쪽: "에이전트는 *엄격한 경계와 예측 가능한 구조* 를 갖춘 환경에서 가장 효과적이다."

본 문서는 *그림이 아닌 기록*이다. 새 합류자(사람·에이전트)가 이 한 파일만으로
시스템의 형태를 이해할 수 있어야 한다.

## 1. 시스템 개요

(시스템이 무엇을 하는지 5 줄 이내. 도메인 용어 그대로 사용.)

## 2. 디렉터리 지도

```
{{PROJECT_ROOT}}/
├── (여기에 주요 디렉터리와 한 줄 설명)
└── ...
```

## 3. 실행 모드 / 컴포넌트 경계

(예: frontend / backend / worker / cron — 각 컴포넌트의 책임과 *서로 어떻게 격리되어 있는가*.
PDF 9쪽 "엄격한 경계" — 어떤 import 가 금지되는지, 그 강제 메커니즘은 무엇인지.)

## 4. 데이터 흐름

(요청이 들어오면 어떤 경로로 흐르는가. 외부 → 내부 → 외부 의 한 방향 다이어그램.)

## 5. 외부 의존성

(DB, 큐, 외부 API, 인증 공급자 — 각각의 *경계 어댑터* 위치.)

## 6. 핵심 불변식 (R1~Rn)

(이 시스템이 절대 위반해서는 안 되는 원칙들. 본문은 docs/design-docs/core-beliefs.md 로 위임.)

| ID | 한 줄 요약 | 강제 메커니즘 | 상세 |
|----|-----------|--------------|------|
| R1 | (예) 모드 격리 | ESLint no-restricted-imports + pytest AST | core-beliefs.md §R1 |
| R2 | ... | ... | ... |

## 7. 외부 채널 → 리포 인코딩 (PDF 6쪽)

> "에이전트의 관점에서 보면 실행 중에 컨텍스트 내에서 액세스할 수 없는 것은 사실상 존재하지 않는다."

다음 외부 지식은 본 리포의 어디에 인코딩되어 있는가:

- Slack 결정 → `docs/audits/`
- 사용자 피드백 → `docs/product-specs/` 또는 `docs/exec-plans/`
- 머릿속 합의 → `docs/design-docs/core-beliefs.md`

(외부에만 있고 인코딩되지 않은 것은 *존재하지 않는 것*. 발견 즉시 인코딩하라.)
