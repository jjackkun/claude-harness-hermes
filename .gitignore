# Local Claude Code settings (per-user permissions, overrides)
.claude/settings.local.json

# Local Codex project settings generated by project-codex.sh
.codex

# Impeccable external installer residue — not used by this repo
.agents/
skills-lock.json
/skills/

# fzf binary — large, platform-specific. Users run scripts/install-fzf.sh
bin/fzf

__pycache__/
*.pyc

# Machine-local registry of installed projects (never push)
.installed-projects
.installed-projects.codex
.worktrees/
.claude/worktrees/


temp/

# Terminal Paste Image folder
.cp-images/

# 개인 작업 노트·외부 저작물 PDF — 공개/공유 대상 아님 (로컬 보관용)
.00_docs/

docs/superpowers/plans/

# >>> harness-agent-preset >>>
# Auto-managed by claude-harness-hermes. Do not edit between markers.
.claude/settings.local.json
.claude/worktrees/
.claude/.review-dirty
.claude/.dev-setting-manifest.json
.claude/.last-install.txt
.claude/presets.lock
.claude/harness-hooks.lock
.harness/
!.claude/memory/
!.claude/memory/**
.hermes/*
!.hermes/skills/
!.hermes/skills/**
!.hermes/factory.json
!.hermes/universe.id
!.hermes/agents.json
!.hermes/agents/
!.hermes/agents/*/
.hermes/agents/*/*
!.hermes/agents/*/SOUL.md
!.hermes/agents/*/skills/
!.hermes/agents/*/skills/**
!.hermes/organization.yaml
.hermes/summons/
# <<< harness-agent-preset <<<

# ── 공장(이 저장소) 전용: 마커 블록 뒤에 와야 예외를 되무시할 수 있다 ─────────────
# 마커 블록은 .hermes/* 를 무시하고 factory.json · skills/ · history/ 를 예외로 푼다.
# 공장은 소우주가 아니다 — 대화 원문(history, 평문)과 결정화 스킬은 코드 저장소에 싣지 않는다
# (설계 raw-transcript.md §3, 계획 3 목표 1). factory.json 만 추적한다.
# 주의: `.hermes/` 를 통째로 무시하면 git 이 하위로 내려가지 않아 예외까지 무효가 된다(리뷰 2026-09-16 HIGH).
.hermes/history/
.hermes/skills/
