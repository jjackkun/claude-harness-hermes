# R-design-cover 승격 감사 — 2026-09-17

> 작성일: 2026-09-17
> 목적: "설계에서 확정된 결정은 계획서에 결정 ID 로 인용돼야 한다" 를 core-beliefs.md 의 R 룰로 승격하고, 판정기·pre-commit 게이트·기준선·테스트를 붙인다.

## 트리거

- 소스: [x] 반복 결함  [ ] 리뷰 지적  [ ] 시스템 신호  [x] 사용자 명령
- 참조:
  - 계획 `completed/2026-09-17-summon-soul-injection.md` — 설계 `creation-and-organization.md:20` 이 계획 4 목표에서 빠져 코드 0.
  - 계획 `completed/2026-09-17-design-coverage-gaps.md` — 설계 9개 문서 전수 대조: 누락 15건 + 계획 ✔이나 코드 0건 1건(RV-07). 두 계획 §8 이 같은 룰 후보를 냈다.
  - 사용자 명령("승격하자", 2026-09-17).

## 패턴 요약

- **증상**: 설계 문서에 "(확정)"·"✅ 리뷰 확정" 으로 굳은 기계 동작이 실행 계획서 §2 목표로 옮겨지지 않은 채 계획이 완료로 닫힌다. 하루에 16건, 보안 1건(이력 자유 글 원문 저장) 포함.
- **근본 원인**: 설계→계획 전사가 사람 손이고, 옮겨졌는지 대조하는 장치가 없었다. 계획 완료 게이트(R-retro·R-acc)는 계획서 안만 본다.
- **영향**: 소환된 에이전트가 정체성을 안 읽음, 이력에 비밀값 원문, 봉투 계약 절반 부재, 사칭 방어 한 겹 부재 — 전부 설계상 "확정" 이었다.

## 실측 (2026-09-17)

| 단위 후보 | 결과 | 채택 |
|---|---|---|
| 줄 번호(`파일:줄`) | 계획서 인용 중 줄 번호 꼴 1건뿐, 편집마다 어긋남 | ✗ |
| 확정 절 제목 문자열 | 61건 중 45 인용·16 미인용, 그 16 대부분 오탐(`done_when` 허용 형식 등 구현됐으나 표현이 다름) | ✗ (G6) |
| 결정 ID | 첫 측정(RV·G·C·J·V·P·I) 47개 중 43 인용; 최종 집합(RV·G·V·K·T·C, 설계 문서에 실제 쓰이는 접두)으로는 미인용 13 → 실제 틈 | ✓ 주 단위 |
| ID 없는 확정 절 | `(확정)` 절 42개 중 ID 있는 것 3 | ✓ 보조 (B): "확정이면 ID" |

## 변경

- `docs/design-docs/core-beliefs.md`: `## R-design-cover {#r-design-cover}` 절(R-coexist 다음).
- `assets/hooks/design_cover.py`(신규): (A) 미인용 결정 ID · (B) ID 없는 확정 절 판정, `.design-cover-baseline` 억제, rc 0/1/2.
- `assets/hooks/pre-commit.sh`: `# GATE: R-design-cover warn` — 설계 문서·계획서 스테이징 시에만, 설계 디렉터리 없으면 skipped. 게이트 18종(차단 11 / 경고 7).
- `.design-cover-baseline`(신규): 도입 시점 틈 71건(ID 13 · 절 58) — 줄어들기만 한다.
- `tests/design-cover-gate-test.sh`(신규): 검출 (A)(B) · 기준선 억제 · 인용 추가로 해소 · rc 2 · 실 저장소 새 틈 0 · 게이트 선언·앵커 존재.
- `.deprc` tier 0 · `tests/run-all.sh` 등록 · 문서 수치 동기.

## 회고

- 이 룰이 있었다면 계획 4 를 닫을 때 "creation-and-organization.md 의 확정 ID 중 인용 없는 것" 이 경고로 떴을 것이다 — 단, SOUL 문장은 ID 가 없어 그때도 못 봤을 것이다. 그래서 규칙 (B)(확정이면 ID)를 같이 올렸다. 룰은 과거를 잡는 게 아니라 앞으로의 확정을 판정기 안으로 밀어 넣는다.
- 기준선 71건은 부채다. 계획서·감사에 ID 를 열거하면 판정기가 '인용됨' 으로 보므로 기준선 파일에만 둔다 — 기준선 생성 직후 그 함정에 한 번 걸렸다(계획서 동기 절의 ID 열거를 지우자 4건이 새 틈으로 떴다). backlog `design-gaps-tier2.md` 와 함께 줄인다 — 항목 하나를 지울 때마다 그 ID 가 실제로 어느 계획서에 인용됐거나 절 제목에 ID 가 붙은 것이다.
