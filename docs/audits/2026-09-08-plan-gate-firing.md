# 계획 축 게이트는 실제로 발화하는가

> 작성일: 2026-09-08
> 목적: `docs/exec-plans/.../2026-09-03-gate-telemetry.md` 목표 4 에 데이터로 답한다.

## 질문

gate-telemetry 계획의 목표 4 는 이렇게 물었다:

> `completed/` 1건뿐인 현 상태의 원인이 "게이트 무발화" 인지 "제외 범위 과대" 인지
> `gate_report.py` 출력으로 판정하고 감사 문서에 기록.

## 답 — 둘 다 아니다. **계장이 없어서 판정 자체가 불가능했다.**

2026-09-08 기준 `.harness/gate-events.jsonl` 176건에 대한 `gate_report.py` 출력에
**`R-plan-missing` 과 `R-plan-stale` 의 행이 아예 없다.**

```
룰              기회  통과  경고  차단  우회  건너뜀  발화율
R-review-debt  1   0   1   0   0   0    100.0%
R5             38  0   38  0   0   0    100.0%
R-iface        3   1   0   2   0   0    66.7%
R-dead-file    2   1   1   0   0   0    50.0%
R-doc          4   2   0   2   0   1    50.0%
R-review       13  9   4   0   0   0    30.8%
R-size-resp    49  35  14  0   0   0    28.6%
R-cx           4   3   0   1   0   0    25.0%
R-declare      1   1   0   0   0   0    0.0%
R-dep          4   4   0   0   0   0    0.0%
R-pipe         7   7   0   0   0   0    0.0%
R-retro        2   2   0   0   0   0    0.0%
R-secret       16  16  0   0   0   0    0.0%
R-size         31  31  0   0   0   0    0.0%
```

`R-plan-stale` 은 이 세션의 커밋 4건 **전부에서 화면에 경고를 찍었다.** 그런데
기록은 0건이다. 화면에는 나오고 기록에는 안 남는 게이트였다.

### 원인

`assets/hooks/pre-commit.sh` §8 이 `WARNINGS+=(...)` 만 하고 `gate_add` 를 부르지
않았다. gate-telemetry 계획의 Step 2b("pre-commit 축 계장")가 7개 게이트를 계장했는데
계획 축 두 개가 그 7개에 들어 있지 않았다.

전수 확인 결과, 선언(`# GATE:`)은 있으나 계장이 없는 게이트가 **8개**였다:

```
R-acc  R-cov  R-fmt  R-lint  R-plan-missing  R-plan-stale  R-struct  R-test
```

즉 게이트 16종 중 절반이 발화율 데이터를 만들지 않고 있었다. 텔레메트리가 있다는
사실 자체가 "관측되고 있다" 는 착각을 만들었다 — 이 저장소가 반복해서 만나는
**조용히 꺼진 게이트** 유형이다.

## 부수 발견 — `R-plan-missing` 은 이 저장소에서 구조적으로 발화할 수 없었다

계장을 붙이고 나서도 `R-plan-missing` 은 발화하지 않을 것이다. 판정 코드가 이렇다:

```bash
if [[ ${#ACTIVE_PLANS[@]} -eq 0 ]]; then      # → R-plan-missing
elif [[ -z "$STAGED_PLANS" ]]; then           # → R-plan-stale
fi
```

`active/` 에 계획서가 **하나라도** 있으면 `R-plan-missing` 분기에 도달하지 않는다.
그런데 이 저장소는 `2026-09-03-gate-telemetry.md` 가 목표 4 미완으로 계속 active 에
남아 있었다. 즉 그 계획서 자신이 자기를 검사하는 게이트를 가리고 있었다.

이것은 결함이 아니라 설계대로다(계획서 1개일 때 차단하면 워킹트리 공유 시 상호
차단이 발생한다 — 2026-07-23 사고). 다만 **"발화하지 않음" 의 의미가 "위반이 없음"
이 아니라 "판정 대상이 아님"** 이라는 점이 기록되지 않았다.

## 조치

1. 계획 축 두 게이트에 `gate_add` 를 붙였다. 세 분기 전부에 붙이되 판정 종류를
   구분한다 — **판정하고 통과한 것은 `pass`, 판정 대상이 아니었던 것은 `skipped`**.
   차단만 기록하면 분모가 없어 "안 걸렸다" 를 알 수 없고, 판정하지 않은 것까지
   `pass` 로 세면 분모가 부풀어 발화율이 실제보다 낮아진다. 이번 감사가 전자로
   답을 못 냈고, 고치는 과정에서 후자를 만들 뻔했다(아래 검토 절 참조).
2. 나머지 미계장 6종(`R-acc` `R-cov` `R-fmt` `R-lint` `R-struct` `R-test`)은
   `docs/exec-plans/backlog/instrument-remaining-gates.md` 로 남겼다.

## 검토에서 잡힌 것 (2026-09-08, code-reviewer)

**계장을 붙이면서 새로운 분모 왜곡을 만들 뻔했다.** 계획이 0건인 분기에서
`R-plan-stale` 을 `pass` 로 기록했는데, 그 상황은 "통과" 가 아니라 "평가된 적 없음" 이다.
`gate_report.py` 의 `_row()` 는 "기회(분모)에서 skipped 는 뺀다 — 판정하지 못한 것은
평가 기회가 아니다" 라고 정의하고, 바로 위 `R-plan` 도 같은 상황에 `skipped` 를 쓴다.
저장소 자신의 스키마 규약을 그 한 줄만 어기고 있었다. `skipped` 로 고쳤다.

**이 감사가 답을 못 냈던 이유와 정확히 대칭이다.** 기록이 없어서 판정을 못 했던 문제를
고치면서, 이번에는 판정하지 않은 것을 통과로 기록해 발화율을 실제보다 낮게 만들 뻔했다.
계장은 "기록을 남기는 것" 이 아니라 "무엇을 판정했고 무엇을 판정하지 않았는지 구분해
남기는 것" 이다.

**모듈 이름 캐시가 사본 드리프트를 숨길 수 있었다.** 시험이 `sys.path.insert` 로
`doc_counts` 를 import 했는데, 같은 이름의 사본(`scripts/hooks/doc_counts.py`)을
대상으로 하는 시험이 나중에 추가되면 먼저 캐시된 쪽이 조용히 재사용된다.
경로를 고정하는 방식(`spec_from_file_location`)으로 바꾸고, **두 사본이 갈라졌는지
자체를 단언하는 시험**을 추가했다 — 숨기지 않는 것만으로는 부족하고 갈라짐을 잡아야 한다.

## 배운 것

**"관측 장치가 있다" 와 "관측되고 있다" 는 다르다.** 텔레메트리를 도입한 계획이
스스로 "발화율 관측 중" 이라고 적었지만, 정작 그 계획의 마지막 질문에 답하려는
순간 대상 게이트가 계장돼 있지 않았다. 계장 여부를 세는 검사가 없으면 이 상태는
다시 발생한다 — 그것이 위 backlog 항목의 내용이다.
