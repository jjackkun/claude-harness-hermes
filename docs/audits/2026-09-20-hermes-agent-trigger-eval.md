# 2026-09-20 — hermes-agent 스킬 발동 평가 실측 (agent-hire-form 목표 6)

> 도구: skill-creator `run_eval.py` / `run_loop.py`(정식 경로, 헤드리스 `claude` 호출은 도구 안에서만). 평가 집합 `assets/skills/hermes-agent/evals/trigger-eval.json` 12 질의(발동 6 · 비발동 6).
> 실행 위치: 저장소 밖 빈 루트 `/tmp/hermes-eval.*` — 저장소 안에서 돌리면 진짜 `hermes-agent` 스킬이 가짜 평가 명령(`hermes-agent-skill-<id>`)을 가려 0% 가 나온다(지난 세션 발견 재확인).
> 원본 결과: `.harness/out/skill-eval/2026-09-20_183739/`(무시 경로, 이 기계에만).

## 1. 결과

| 실행 | 설명 | 정밀도 | 재현율 | 정확도 |
|---|---|---|---|---|
| run_eval 12질의 × 3회, 45초 | 현재 설명(507자) | 100% | 28% | 50% (6/12 통과: 비발동 6 전부, 발동 0) |
| run_eval 발동 6질의 × 5회, 90초 | 현재 설명 | — | 37% (11/30) | 1/6 통과 |
| run_loop 반복 1 (train 8 / test 4) | 현재 설명 | 100% | 25% / 33% | 62% / 67% |
| run_loop 반복 2 | sonnet 개선안 (618자, 의도 7가지 번호 나열) | 100% | 25% / 33% | 62% / 67% |
| run_loop 반복 3 | sonnet 개선안 (560자) | 100% | 17% / 33% | 58% / 67% |

루프 판정: 최고 = 반복 1(현재 설명). 개선안 둘 다 점수를 올리지 못했다. **설명은 바꾸지 않는다.**

## 2. 읽기

- **오발동 0** — 비발동 6 질의(온보딩 문서 작성, 서브에이전트 model 필드 수정, yaml 파서 테스트, git log 정리, 헤르메스 루프, QA 체크리스트)는 세 설명 모두 0/3. "직원·QA·에이전트" 낱말이 겹쳐도 다른 일이면 안 잡는다 — 목표 6 이 노린 근접 사례 방어는 됐다.
- **재현율 한계는 평가 방식** — 근거: 슬래시 명령 그 자체인 `/hermes-agent` 가 세 반복 모두 0/3. 평가는 명령을 `hermes-agent-skill-<id>` 로 바꿔 심으므로 사용자가 친 이름과 맞을 수 없다. 나머지 발동 질의도 빈 루트(CLAUDE.md·조직 파일·명부 없음)에서 "이 프로젝트에 직원 뽑자" 를 받는 것이라, 모델이 "여기엔 명부가 없다" 로 답하고 끝내기 쉽다. 설명 문구를 두 번 크게 바꿔도 질의별 발동 횟수가 거의 같았다(1/3 ↔ 0~2/3).
- 실제 소우주에서는 세션마다 `[Hermes 관련 규칙]` 로 스킬 설명이 프롬프트에 붙고 진짜 스킬 이름이 있어 조건이 다르다. 그쪽 발동률은 이 도구로는 잴 수 없다(가림 문제) — 재려면 진짜 스킬 이름으로 픽스처 프로젝트 안에서 도는 평가기가 필요하다(backlog `skill-trigger-eval-in-project`).

## 3. 결정

- 목표 6 은 "측정함 · 임계(발동률 50%) 미달 · 원인은 평가 방식" 으로 닫는다. 설명은 현재 것을 유지한다(정밀도 100%, 루프 최고).
- 다음 측정은 backlog 의 프로젝트 안 평가기가 생긴 뒤에 한다.
