# 2026-09-23-test-suite-parallel — 전체 시험 묶음을 병렬로 돌린다

## 1. 동기 (Why)

**실측 2026-09-23**: `bash tests/run-all.sh` 107개가 **35분**(09:41 → 10:16, 107/107 통과).
백로그 `test-suite-parallel.md` 참조. 느린 시험 하나가 아니라 구조다 — 17개가
`project-claude.sh` 전체 설치(파일 95개 복사 + settings·CLAUDE.md 생성)를 하고 **순차로** 돈다.

진짜 비용은 35분이 아니라 **"도는 동안 저장소를 못 고친다 × 반복 횟수"** 다.
한 세션에서 4번 돌려 3번을 중간에 죽였다(도중에 파일을 고쳐 결과가 무의미해짐).
사용자가 "26분째 하고 있어. 이게 맞아?" 라고 물었다.

## 2. 목표 (검증 가능한 형태)

- [x] 목표 1 — `HARNESS_TEST_JOBS=1`(기본)이면 **동작이 지금과 완전히 같다**.
      검증: `tests/run-all-parallel-test.sh` — 기본값에서 출력 형식(`── RUN/PASS/FAIL:`)과
      요약 줄이 그대로이고, 실패 1건을 심으면 rc 1 · 실패 목록에 그 이름.
- [x] 목표 2 — `HARNESS_TEST_JOBS=N`(N>1)이면 **판정 집합이 같다**.
      검증: 같은 시험 — 일부러 깨뜨린 시험 1개를 포함한 픽스처 목록을 순차/병렬로 돌려
      통과·실패 이름 집합이 일치.
- [x] 목표 3 — 병렬에서도 **한 시험의 출력이 통째로** 읽힌다(섞이지 않는다).
      검증: 같은 시험 — 두 시험이 각각 여러 줄을 내도록 하고, 각 `── RUN: X ──` 와
      `── PASS/FAIL: X ──` 사이에 다른 시험의 줄이 끼지 않음을 단언.
- [ ] 목표 4 — 실제 묶음이 **10분 이내**. 검증: `time bash tests/run-all.sh` (JOBS 기본을 N 으로 올린 뒤).

## 2-bis. 착수 전 확인한 사실 (2026-09-23)

| 확인한 것 | 결과 |
| --- | --- |
| 코어 수 | 22 |
| 시험 수 | 104 파일 · 등록 107 단계 |
| 전체 설치를 하는 시험 | 17개 |
| 실 저장소(`$REPO_ROOT`)에 **쓰는** 시험 | **0개** — 13건의 hit 은 전부 `cp $REPO_ROOT → $TMP`(읽기) |
| `HOME` 을 치환하지 않는 시험 | 62 / 104 |
| 그중 `~/.hermes`·`global.db`·`$HOME` 을 실제로 건드리는 것 | **0개** — 3건은 주석·단언 문구뿐 |
| 실 저장소를 **읽어 불변을 단언**하는 canary | `copy-install-test.sh:170`(`.installed-projects`) · `hermes-dashboard-test.sh`(우주 대시보드 mtime) |
| 러너 구조 | `REGISTERED_TESTS` 배열 → 271줄 단일 루프가 `run_step` 순차 호출 |
| `run_step` 출력 | CI 에서만 `tee` 로 사본, 로컬은 stdout 직결 → **병렬이면 섞인다** |

## 3. 비목표

- 시험을 지우거나 단언을 줄여 시간을 버는 것. 107개는 이 세션에서 실제로 결함 3건을 잡았다.
- 설치 자체(파일 95건 복사)를 빠르게 만드는 것 — 별건, 위험이 크다.
- §1 정적 검사·§2 무결성 검사의 병렬화. 그것들은 이미 빠르고 서로 순서가 있다.
- CI 이관. CI 는 60회 연속 실패한 이력이 있다 — 로컬에서 돌 수 있어야 한다.

## 4. 영향 영역

- 코드: `tests/run-all.sh` — `run_step` 의 출력 수집 + `REGISTERED_TESTS` 루프를 작업자 풀로
- **신규 파일 목록 (파일별 책임 1줄)**
  - `tests/run-all-parallel-test.sh` — 러너의 병렬 모드가 순차와 **같은 판정·온전한 출력**을 내는지 단언한다.
- 룰: 없음. 데이터: 없음. 외부 의존: 없음(bash `wait -n`, 4.3+).
- 전파: `tests/` 는 공장 전용이라 소우주 전파 없음.

## 5. 단계

### Step 1. 시험 먼저 (RED)
- 산출: `tests/run-all-parallel-test.sh` + `tests/run-all.sh` 등록(고아 검사 통과용)
- 검증: 목표 1~3 이 실패한다

### Step 2. 출력 수집 (목표 3)
- 산출: `run_step` 이 항상 파일로 모으고, 끝난 뒤 한 번에 뱉는다(CI 의 `tee` 경로와 합침)
- 검증: 목표 3 초록, 목표 1 유지

### Step 3. 작업자 풀 (목표 2)
- 산출: `HARNESS_TEST_JOBS`(기본 1). N>1 이면 271줄 루프가 풀로 돌고, **원래 순서대로** 출력·집계
- 검증: 목표 1·2 초록

### Step 4. 기본값 올리고 실측 (목표 4)
- 산출: 기본 JOBS 를 실측으로 정한다(무작정 `nproc` 아님 — I/O 바운드)
- 검증: `time bash tests/run-all.sh` 3회 중앙값 · 전체 107/107

## 6. 의사결정 로그

- 2026-09-23: **pyenv 우회를 설치기(`lib/common.sh`)에도 넣었다** — 근거: 사용자 "실제를 고쳐야 할 거 아냐".
  시험만 빨라지고 실제 `update-all` 은 그대로였다. 틀렸을 때 손해: 설치기가 셔임과 다른 파이썬을 쓸 수 있다
  (→ 실행 폴더 기준으로 막았다).

- 2026-09-23: **기본값을 1 로 두고 시작한다** — 근거: 러너가 깨지면 모든 검증 수단을 잃는다.
  동작 동일을 시험으로 고정한 뒤에 올린다. 틀렸을 때 손해: Step 4 까지 가야 이득이 난다.
- 2026-09-23: **직렬 강제 목록을 만들지 않는다** — 근거: 실측에서 저장소에 쓰는 시험 0,
  `$HOME` 을 실제로 건드리는 시험 0. 근거 없이 목록을 만들면 그 목록이 썩는다.
  틀렸을 때 손해: 병렬에서 간헐 실패가 나면 그때 목록을 도입해야 한다(그 판단 근거는 실패 로그).
- 2026-09-23: **`wait -n` 을 쓴다** — 근거: bash 4.3+ 표준, 외부 의존(GNU parallel·xargs -P) 없음.
  틀렸을 때 손해: 아주 옛 bash 에서 못 돈다(이 저장소는 이미 bash 4.3+ 문법을 쓴다).

## 7. 발견·예외

- **35분의 진짜 원인은 병렬이 아니라 pyenv shim 이었다.** 설치 1회가 python3 를 286번 부르고,
  이 환경의 shim 은 호출마다 버전을 따지느라 112ms(실제 19ms)를 썼다 — 설치 36.6초 중 26초.
  묶음 35분 ≒ 설치 58회 × 36초. 그래서 범위가 늘었다: `lib/pyenv_bypass.sh` 로 설치기 자체를
  빠르게 했다(설치 36.6 → 10.6초). 병렬만 했으면 원인을 가린 채 시간을 나눴을 것이다.
- **매니페스트 모아 쓰기(설치 10.5 → 5.0초)는 되돌렸다.** `install-coexist` 12건을 깨뜨렸고 원인을
  못 찾았다(버퍼 경로 해싱·커밋 캐시 두 가설 모두 기각). 별건으로 남긴다.
- **우회를 대상 폴더 기준으로 바꿨다가 되돌렸다.** 대상의 `.python-version` 이 옛 버전이면 공장의
  설치 도구가 그 버전으로 돌다 깨진다. shim 과 같게 실행 폴더 기준이 맞다.
- 코드 리뷰 3회, 지적 8건. 그중 2건은 내가 쓴 시험이 **구조적으로 못 잡는** 결함이었다
  (기본 모드 출력 변화 · 병렬 속도 단언의 흔들림).

## 8. 회고 (완료 시 작성)

- 잘된 것:
- 잘못된 것:
- 다음 룰 후보:
