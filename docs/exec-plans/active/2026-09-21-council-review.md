# 2026-09-21-council-review — 설계 결정에 반대편을 한 명 붙인다

> 출처: backlog `council-review` (ECC 결핍 #9, 가치 중) → 2026-09-21 사용자 "진행하자".
> 착수 조건이 문서에 박혀 있다: **"한 번 시험해 본 뒤 유지 여부 결정. 사람이 먼저 '반론이 유용했다' 고 말해야 남긴다."**
> 그래서 이 계획은 만들고 끝이 아니라 **실제 문서에 돌려 결과를 사람 앞에 놓는 것**까지가 범위다.

## 1. 동기 (Why)

설계 문서는 `architect-lite` 한 시선으로만 검토된다(R7). 그 에이전트는 **빠진 것**을 찾는다 — 제약·대안·롤백이 적혔는지.
"적힌 것이 틀렸을 가능성" 을 맡는 자리는 없다. 쓴 사람과 검토자가 같은 방향을 보면 결정은 확정 편향으로 굳는다.

오늘만 해도 측정 하나가 결론을 뒤집었다(`gateguard-fact-force`: 정의를 바꾸자 18.4% → 95.7%).
그때 반대편을 맡은 시선이 있었다면 정의를 더 일찍 의심했을 것이다.

## 2. 목표 (What — 검증 가능한 형태)

- [~] 목표 1 — 반대편만 맡는 에이전트가 있다. 검증: `test -f assets/agents/devil-advocate.md` + `grep -c devil-advocate presets/workflow/harness.conf` ≥ 1. **파일은 있음, 등록은 목표 4 판단 뒤.**
- [x] 목표 2 — **만들어낸 반론을 막는 장치**가 규격 안에 있다. 검증: `grep -c "반증 조건" assets/agents/devil-advocate.md` ≥ 1. 반론마다 인용과 "이 반론이 틀렸다면 무엇을 보면 아나"(반증 조건)를 요구하고, 반론이 없으면 `반론 없음` 을 쓰게 한다.
- [x] 목표 3 — 실제 문서 2건에 돌린 결과가 남는다. 검증: `grep -c "원문" docs/audits/2026-09-21-devil-advocate-trial.md` ≥ 2.
- [ ] 목표 4 — 유지 여부는 **사람이 정한다**. 검증: `grep -c "사용자 판단" docs/exec-plans/active/2026-09-21-council-review.md` ≥ 2 (§6 에 기록). 그 전까지 R7 표를 고치지 않는다.

## 2-bis. 착수 전 확인한 사실 (2026-09-21)

| 확인한 것 | 결과 |
| --------- | ---- |
| 기존 설계 검토 경로 | `assets/agents/architect-lite.md` 1개 — "빠진 것" 만 본다(Findings: HIGH/MEDIUM) |
| R7 표 | `*-design.md`·`*-spec.md` → architect-lite / `*-plan.md` → planner-lite. 반대편 자리 없음 |
| dispatch 차단 여부 | `claude-pretooluse-agent-guard.sh` 는 **soft warning** — 컨텍스트 주입만, 차단 아님 |
| 에이전트 자산 수 | `assets/agents/` 15개 |
| 시험 대상 후보 | `docs/design-docs/agent-eval-llm-path.md`(설계) · `docs/audits/2026-09-21-gateguard-fact-force-measurement.md`(오늘 내린 결정) |

## 3. 비목표 (Out of Scope)

- **자동 실행 금지.** R7 에 넣더라도 "붙일까요?" 를 묻는 행까지다. 설계 문서를 저장할 때마다 에이전트가 자동으로 도는 것은 만들지 않는다.
- 다중 모델 반론(Codex 경유) — R3(구독 CLI 단일 경로) 안에서만 가능하고, 단일 모델 반론이 쓸모 있다고 판명된 뒤의 별건.
- 네 목소리(찬성·반대·비용·운영) 구조 — 찬성은 이미 문서 저자가 맡는다. 반대 하나만 붙인다.

## 4. 영향 영역

- 코드: `presets/workflow/harness.conf`(에이전트 복사 목록), 문서 개수 동기화
- **신규 파일 목록**:
  - `assets/agents/devil-advocate.md` — 설계 결정의 반대편만 맡는 서브에이전트 (다른 책임 없음)
  - `docs/audits/2026-09-21-devil-advocate-trial.md` — 실제 2건 시험 결과 원문과 사람의 판단 자리
- 룰: R7(제안 표) — **사람 판단 후에만** 수정.
- 데이터·외부 의존: 없음.

## 5. 단계 (Steps)

### Step 1. 에이전트 규격 [Impl]
- 검증: 반론마다 인용·반증 조건 요구, `반론 없음` 허용이 본문에 있다

### Step 2. 실제 2건에 돌린다 [Impl]
- 입력: 설계 문서 1건 + 오늘 내린 결정 1건
- 검증: 출력 원문을 감사 문서에 그대로 보존(요약하지 않는다 — 판단은 사람이 한다)

### Step 3. 사람 판단 [보류]
- "유용했다" 면 R7 표에 행 추가, 아니면 자산을 지우고 백로그에 사유를 남긴다

## 6. 의사결정 로그

- 2026-09-21: 찬성·비용·운영 목소리는 만들지 않고 **반대 하나만** 만든다 — 근거: 찬성은 저자가, 비용·운영은 architect-lite 의 기존 항목이 이미 맡는다. 겹치는 역할을 늘리면 검토가 길어지고 사람이 안 읽는다.
- 2026-09-21: 반론마다 **반증 조건**을 요구한다 — 근거: 반대만 맡는 역할은 반론을 지어내기 쉽다. 틀렸을 때 무엇을 보면 아는지 못 쓰는 반론은 소음이다.

## 7. 발견·예외

- **반증 조건이 제 몫을 했다.** 두 시험에서 반론 5개(검증 가능한 것)가 나왔고, 반증 조건을 그 자리에서 돌려 4개 성립·1개 무너짐.
  무너진 반론(gateguard "봤다" 정의 과대 집계)은 전수 재집계로 **미노출 명령 0.3%** 가 나와 10분 만에 기각됐다.
  반증 조건이 없었다면 "일리 있다" 로 남아 결정을 흔들기만 했을 것이다.
- **실제로 바뀐 것 두 가지.** (1) gateguard 감사 문서가 계측기로 재현 불가한 수치(세션 내 순번)를 출처 없이 싣고 있었다 — 출처를 밝혔다.
  (2) harness-eval 이 **아무것에도 불리지 않는다**는 사각지대를 찾았다 — 그 도구를 만든 이유("두 달 몰랐다")를 그대로 상속한 모양. `backlog/harness-eval-trigger-gap.md`.
- 반대편 역할이 판정을 둘 다 "재검토 권고" 로 냈다. `반론 없음` 은 한 번도 나오지 않았다 — 2건으로는 과잉 반론 성향인지 판단할 수 없다.
- 이 세션에서는 새 에이전트 타입이 시작 시점 등록이라 **규격을 프롬프트로 주입**해 돌렸다. 시험 도중 타입이 등록됐다(다음 호출부터는 `subagent_type: devil-advocate` 로 부를 수 있다).

## 8. 회고 (완료 시 작성)

- 잘된 것:
- 잘못된 것:
- 다음 룰 후보:
