# <YYYY-MM-DD-slug> — <실행 계획 제목>

PDF 5~6쪽: "복잡한 작업은 진행 상황과 의사결정 로그와 함께 실행 계획에 포함되어
리포지터리에 저장된다. 진행 중인 계획, 완료된 계획, 그리고 알려진 기술 부채는
모두 버전화되어 관리되고 동일한 위치에 배치되어 *에이전트가 외부 상황에 의존하지 않고*
작업할 수 있다."

본 파일을 `docs/exec-plans/active/YYYY-MM-DD-<slug>.md` 로 복사해 시작.
완료 시 `docs/exec-plans/completed/` 로 이동 (삭제 X — 의사결정 로그가 보존되어야 함).
아직 착수하지 않은 계획은 `docs/exec-plans/backlog/` 에 둔다.

---

## 1. 동기 (Why)

(이 작업이 *왜* 지금 필요한가. 사고/요구/관찰 등 트리거가 된 사실. 의견 X 사실 O.)

## 2. 목표 (What — 검증 가능한 형태)

- [ ] 목표 1 — 검증 방법:
- [ ] 목표 2 — 검증 방법:

## 3. 비목표 (Out of Scope)

- 이번 계획에서 *건드리지 않는* 것. 명시해 두면 범위 폭증을 막는다.

## 4. 영향 영역

- 코드: (디렉터리/파일 수정 목록)
- **신규 파일 목록 (파일별 책임 1줄 필수)**: ← 비워두지 말 것. 1파일=1책임 원칙 강제.
  - `path/to/file.ext` — <이 파일의 유일한 책임 한 문장>
  - `path/to/barrel/index.ts` — 배럴 재export (있을 경우)
  - (위 목록을 먼저 못 쓰면 아직 설계가 덜 됐다는 뜻. 구현 들어가지 말 것.)
- 룰: (R 번호)
- 데이터: (마이그레이션 여부)
- 외부 의존: (있다면 어떤 호환성 고려)

## 5. 단계 (Steps)

각 단계는 *원자적이고 검증 가능하게*. 비자명한 단계는 Plan→Impl→Review 샌드위치 지정.

### Step 1. <제목> [Plan/Impl/Review 또는 단순]

- 입력:
- 산출:
- 검증:

### Step 2. ...

## 6. 의사결정 로그

(작업 중 *비자명한* 결정이 발생할 때마다 여기 한 줄로 추가. 날짜·결정·근거.)

- YYYY-MM-DD: <결정> — 근거: <한 줄>

## 7. 발견·예외

(계획에 없던 발견. 다음 계획·룰·design-doc 으로 어떻게 흘려보낼지 표시.)

## 8. 회고 (완료 시 작성)

- 잘된 것:
- 잘못된 것:
- 다음 룰 후보:
